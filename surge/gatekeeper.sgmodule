#!name=Gatekeeper 自动加白
#!desc=在网络切换时及每 3 分钟，将 Surge 当前出口 IP 所属网段自动加入 Gatekeeper 白名单，并提供手动刷新面板。
#!category=Gatekeeper
#!author=dingding229
#!arguments=domain:"",url:"",key:""
#!arguments-desc=domain: Gatekeeper 域名（不含 https://）；url: 完整站点地址（例如 https://allowlist.example.com）；key: awl_ 开头的 API Key

[Rule]
# 必须直连，确保服务端识别到本机真实出口 IP，而不是代理节点 IP。
DOMAIN,{{{domain}}},DIRECT,extended-matching

[Script]
gatekeeper-cron = type=cron,cronexp="*/3 * * * *",script-path=https://raw.githubusercontent.com/dingding229/gatekeeper-allowlist/main/surge/gatekeeper.js,timeout=30,argument="url={{{url}}}&key={{{key}}}"
gatekeeper-event = type=event,event-name=network-changed,script-path=https://raw.githubusercontent.com/dingding229/gatekeeper-allowlist/main/surge/gatekeeper.js,timeout=30,argument="url={{{url}}}&key={{{key}}}"
gatekeeper-panel = type=generic,script-path=https://raw.githubusercontent.com/dingding229/gatekeeper-allowlist/main/surge/gatekeeper.js,timeout=30,argument="url={{{url}}}&key={{{key}}}"

[Panel]
gatekeeper_manual_panel = title="Gatekeeper 自动加白",content="点击右上角刷新按钮手动加白",style=info,script-name=gatekeeper-panel,update-interval=180
